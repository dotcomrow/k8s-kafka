# ============================================================================
# Namespace & RBAC (namespace: kafka)
# ============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: kafka
  labels:
    app: bq-yb-bootstrap
  annotations:
    linkerd.io/inject: "disabled"
    scheduler.alpha.kubernetes.io/node-selector: "dedicated=ctrl-node"
    field.cattle.io/projectId: "local:infrastructure"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bq-yb-bootstrap
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: bootstrap-ok-writer
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get","list","create","update","patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: bootstrap-ok-writer
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
subjects:
  - kind: ServiceAccount
    name: bq-yb-bootstrap
    namespace: kafka
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: bootstrap-ok-writer

# ============================================================================
# Kafka cluster.id generator — namespace: kafka
#   Creates/keeps Secret kafka-cluster-id (key: CLUSTER_ID)
#   Uses Alpine + Kubernetes API (no kubectl, no internet fetches)
# ============================================================================
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kafka-clusterid-gen
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kafka-clusterid-secret-writer
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get","list","watch","create","update","patch","delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kafka-clusterid-secret-writer
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "0"
subjects:
  - kind: ServiceAccount
    name: kafka-clusterid-gen
    namespace: kafka
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kafka-clusterid-secret-writer
---
apiVersion: batch/v1
kind: Job
metadata:
  name: gen-kafka-clusterid
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: kafka-clusterid-gen
      containers:
        - name: gen
          image: alpine:3.19
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -euo pipefail
              API="https://kubernetes.default.svc"
              NS="kafka"
              NAME="kafka-cluster-id"
              TOKEN="$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)"
              CACERT="/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"

              # GET existing secret (if any)
              JSON="$(wget -qO- --header="Authorization: Bearer ${TOKEN}" --ca-certificate="${CACERT}" \
                     "${API}/api/v1/namespaces/${NS}/secrets/${NAME}" || true)"

              # If it exists and has non-empty data.CLUSTER_ID, keep it and exit
              if [ -n "${JSON}" ] && echo "${JSON}" | grep -q '"kind":"Secret"'; then
                if echo "${JSON}" | grep -q '"CLUSTER_ID":"'; then
                  B64="$(echo "${JSON}" | sed -n 's/.*"CLUSTER_ID":"\([^"]*\)".*/\1/p' | head -n1)"
                  if [ -n "${B64}" ] && [ "${B64}" != "null" ]; then
                    echo "kafka-cluster-id already present; nothing to do."
                    exit 0
                  fi
                fi
              fi

              # Generate Kafka-style base64url id (22 chars)
              CID="$(head -c 16 /dev/urandom | base64 | tr '+/' '-_' | tr -d '=[:space:]' | cut -c1-22)"
              echo "Generated cluster.id: ${CID}"

              # Build Secret JSON (data must be base64-encoded)
              if base64 --help 2>&1 | grep -q -- "-w"; then
                ENC="$(printf '%s' "${CID}" | base64 -w0)"
              else
                ENC="$(printf '%s' "${CID}" | base64)"
              fi
              cat > /tmp/secret.json <<EOF
              {
                "apiVersion": "v1",
                "kind": "Secret",
                "metadata": { "name": "${NAME}", "namespace": "${NS}" },
                "type": "Opaque",
                "data": { "CLUSTER_ID": "${ENC}" }
              }
              EOF

              # Create or replace (delete then create if it already exists without value)
              if [ -n "${JSON}" ] && echo "${JSON}" | grep -q '"kind":"Secret"'; then
                wget -qO- --method=DELETE --header="Authorization: Bearer ${TOKEN}" --ca-certificate="${CACERT}" \
                  "${API}/api/v1/namespaces/${NS}/secrets/${NAME}" >/dev/null 2>&1 || true
              fi
              wget -qO- --header="Authorization: Bearer ${TOKEN}" --header="Content-Type: application/json" \
                --post-file=/tmp/secret.json --ca-certificate="${CACERT}" \
                "${API}/api/v1/namespaces/${NS}/secrets" >/dev/null

# ============================================================================
# Vault CONFIG job — namespace: vault
#   Static DB username; password read from secret/<user>:value in Vault KV
# ============================================================================
---
apiVersion: batch/v1
kind: Job
metadata:
  name: yb-vault-config
  namespace: vault
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  backoffLimit: 0
  template:
    metadata:
      annotations:
        linkerd.io/inject: "disabled"
    spec:
      restartPolicy: Never
      serviceAccountName: default
      volumes:
        - name: root-token
          secret:
            secretName: vault-root-token
      containers:
        - name: setup-yb-vault
          image: hashicorp/vault:1.15.4
          env:
            - name: VAULT_ADDR
              value: "http://vault.vault.svc.cluster.local:8200"
            - name: AUTH_PATH
              value: "kubernetes"
            - name: PG_USER
              value: "vaultadmin"   # static username
            - name: SA_NAME
              value: "bq-yb-bootstrap"
            - name: SA_NAMESPACE
              value: "kafka"
            - name: YB_MASTER_ADDRESSES
              value: "yb-master-0.yugabyte.svc.cluster.local:7100,yb-master-1.yugabyte.svc.cluster.local:7100,yb-master-2.yugabyte.svc.cluster.local:7100"
            - name: BQ_BOOTSTRAP_LOCATION
              value: "US"
            - name: BQ_BOOTSTRAP_PROJECT
              value: "k8s-kafka-774d"
            - name: BQ_PROJECT
              value: "k8s-kafka-774d"
            - name: BQ_BOOTSTRAP_BUCKET
              value: "suncoast-systems-yb-bootstrap"
            - name: BQ_DEFAULT_DATASET
              value: "yugabyte_backup"
          command: ["/bin/sh","-c"]
          args:
            - |
              set -euo pipefail
              PG_HOST="${PG_HOST:-yb-tserver-service.yugabyte.svc.cluster.local}"
              PG_PORT="${PG_PORT:-5433}"
              PG_DATABASE="${PG_DATABASE:-kafka}"
              PG_USER="${PG_USER:-vaultadmin}"
              YB_MASTER_ADDRESSES="${YB_MASTER_ADDRESSES:-}"
              BQ_BOOTSTRAP_PROJECT="${BQ_BOOTSTRAP_PROJECT:-}"
              BQ_BOOTSTRAP_BUCKET="${BQ_BOOTSTRAP_BUCKET:-}"
              BQ_BOOTSTRAP_LOCATION="${BQ_BOOTSTRAP_LOCATION:-}"
              BQ_PROJECT="${BQ_PROJECT:-}"
              BQ_DEFAULT_DATASET="${BQ_DEFAULT_DATASET:-}"

              # Tools
              if command -v apk >/dev/null 2>&1; then
                apk add --no-cache jq curl postgresql15-client netcat-openbsd >/dev/null
              else
                apt-get update -y >/dev/null
                apt-get install -y jq curl postgresql-client netcat-openbsd >/dev/null
                rm -rf /var/lib/apt/lists/*
              fi

              # Vault root token
              until [ -s /vault-token/VAULT_TOKEN ]; do sleep 2; done
              export VAULT_TOKEN="$(cat /vault-token/VAULT_TOKEN)"

              # Optional: GCP SA JSON synced into Vault
              SYNCED="$(vault kv get -format=json "secret/k8s-kafka-gcp-service-account-json" 2>/dev/null || true)"
              BQ_SA_JSON="$(printf '%s' "$SYNCED" | jq -r '.data.data.value // empty')"
              : "${BQ_SA_JSON:=}"

              # Wait for static user's password in Vault KV (secret/<PG_USER>: value)
              VAULTADMIN_USER="${PG_USER}"
              VAULTADMIN_PASS=""
              while :; do
                OUT="$(vault kv get -format=json "secret/${VAULTADMIN_USER}" 2>/dev/null || true)"
                if [ -n "${OUT}" ]; then
                  VAULTADMIN_PASS="$(printf '%s' "$OUT" | jq -r '.data.data.value // empty')"
                  [ -n "${VAULTADMIN_PASS}" ] && break
                fi
                sleep 5
              done
              export PGPASSWORD="$VAULTADMIN_PASS"

              # Ensure DB exists
              until getent hosts "$PG_HOST" >/dev/null 2>&1; do sleep 3; end
              until nc -z -w1 "$PG_HOST" "$PG_PORT"; do sleep 3; done
              DB_EXISTS="$(psql -h "$PG_HOST" -p "$PG_PORT" -U "$VAULTADMIN_USER" -d postgres -Atc \
                "SELECT 1 FROM pg_database WHERE datname='${PG_DATABASE}'" || true)"
              if [ "${DB_EXISTS}" != "1" ]; then
                createdb -h "$PG_HOST" -p "$PG_PORT" -U "$VAULTADMIN_USER" -O "$VAULTADMIN_USER" "$PG_DATABASE"
              fi

              # KV seeds
              put_pgconfig() {
                local args=""
                [ -n "$PG_HOST" ] && args="$args host=$PG_HOST"
                [ -n "$PG_PORT" ] && args="$args port=$PG_PORT"
                [ -n "$PG_DATABASE" ] && args="$args database=$PG_DATABASE"
                [ -n "$PG_USER" ] && args="$args user=$PG_USER"
                [ -n "$YB_MASTER_ADDRESSES" ] && args="$args master_addresses=$YB_MASTER_ADDRESSES"
                [ -n "$args" ] && vault kv put secret/kafka/pgconfig $args >/dev/null
              }
              put_bq_bootstrap() {
                local args=""
                [ -n "$BQ_BOOTSTRAP_BUCKET" ]   && args="$args bucket=$BQ_BOOTSTRAP_BUCKET"
                [ -n "$BQ_BOOTSTRAP_PROJECT" ]  && args="$args project=$BQ_BOOTSTRAP_PROJECT"
                [ -n "$BQ_BOOTSTRAP_LOCATION" ] && args="$args location=$BQ_BOOTSTRAP_LOCATION"
                [ -n "$args" ] && vault kv put secret/kafka/bq/bootstrap $args >/dev/null
              }
              put_bq_config() {
                local args=""
                [ -n "$BQ_PROJECT" ]         && args="$args project=$BQ_PROJECT"
                [ -n "$BQ_DEFAULT_DATASET" ] && args="$args dataset=$BQ_DEFAULT_DATASET"
                if [ -n "$BQ_SA_JSON" ]; then
                  printf '%s' "$BQ_SA_JSON" > /tmp/sa.json
                  args="$args sa_json=@/tmp/sa.json"
                fi
                [ -n "$args" ] && vault kv put secret/kafka/bq/config $args >/dev/null
              }
              put_bq_exporter_key() {
                if [ -n "$BQ_SA_JSON" ]; then
                  printf '%s' "$BQ_SA_JSON" > /tmp/bq-exporter.json
                  vault kv put secret/kafka/bq/exporter sa_json=@/tmp/bq-exporter.json >/dev/null
                fi
              }
              put_pgconfig
              put_bq_bootstrap
              put_bq_config
              put_bq_exporter_key

              # Policy & K8s auth
              vault policy write "data-bootstrap" - <<EOP
              path "secret/data/kafka/pgconfig"     { capabilities = ["read"] }
              path "secret/data/kafka/bq/bootstrap" { capabilities = ["read"] }
              path "secret/data/kafka/bq/config"    { capabilities = ["read"] }
              path "secret/data/kafka/bq/exporter"  { capabilities = ["read"] }
              path "secret/data/${PG_USER}"         { capabilities = ["read"] }
              EOP
              vault write auth/${AUTH_PATH}/role/data-bootstrap \
                bound_service_account_names="${SA_NAME}" \
                bound_service_account_namespaces="${SA_NAMESPACE}" \
                policies="data-bootstrap" \
                ttl="24h" >/dev/null

              echo "Configured Vault seeds and policy."
          volumeMounts:
            - name: root-token
              mountPath: /vault-token
              readOnly: true
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "200m", memory: "256Mi" }

# ============================================================================
# One-shot Backfill Job (wave 2) — namespace: kafka
# ============================================================================
---
apiVersion: batch/v1
kind: Job
metadata:
  name: bq-to-yb-bootstrap
  namespace: kafka
  labels: { app: bq-yb-bootstrap }
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels: { app: bq-yb-bootstrap }
      annotations:
        linkerd.io/inject: "disabled"
        vault.hashicorp.com/agent-inject: "enabled"
        vault.hashicorp.com/role: "data-bootstrap"
        vault.hashicorp.com/agent-inject-secret-gcp-key.json: "secret/data/kafka/bq/exporter"
        vault.hashicorp.com/agent-inject-template-gcp-key.json: |
          {{- with secret "secret/data/kafka/bq/exporter" -}}
          {{ .Data.data.sa_json }}
          {{- end -}}
        vault.hashicorp.com/agent-inject-secret-bootstrap.env: "secret/data/kafka/bq/bootstrap"
        vault.hashicorp.com/agent-inject-template-bootstrap.env: |
          {{- with secret "secret/data/kafka/bq/bootstrap" -}}
          {{ with .Data.data.bucket }}BUCKET={{ . }}{{ end }}
          {{ with .Data.data.project }}PROJECT={{ . }}{{ end }}
          {{ with .Data.data.location }}LOCATION={{ . }}{{ end }}
          {{- end -}}
        vault.hashicorp.com/agent-inject-secret-pg.env: "secret/data/kafka/pgconfig"
        vault.hashicorp.com/agent-inject-template-pg.env: |
          {{- with secret "secret/data/kafka/pgconfig" -}}
          {{ with .Data.data.host }}PGHOST={{ . }}{{ end }}
          {{ with .Data.data.port }}PGPORT={{ . }}{{ end }}
          {{ with .Data.data.database }}PGDATABASE={{ . }}{{ end }}
          {{ with .Data.data.user }}PGUSER={{ . }}{{ end }}
          {{ with .Data.data.master_addresses }}YB_MASTER_ADDRESSES={{ . }}{{ end }}
          {{- end -}}
        vault.hashicorp.com/agent-inject-secret-pg-admin.env: "secret/data/vaultadmin"
        vault.hashicorp.com/agent-inject-template-pg-admin.env: |
          {{- with secret "secret/data/vaultadmin" -}}
          PGPASSWORD={{ .Data.data.value }}
          {{- end -}}
    spec:
      serviceAccountName: bq-yb-bootstrap
      restartPolicy: Never
      volumes:
        - name: vault-secrets
          emptyDir: {}
      containers:
        - name: runner
          image: google/cloud-sdk:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /vault/secrets/gcp-key.json
          volumeMounts:
            - name: vault-secrets
              mountPath: /vault/secrets
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -eu
              for f in /vault/secrets/bootstrap.env /vault/secrets/pg.env /vault/secrets/pg-admin.env /vault/secrets/gcp-key.json; do
                i=0; while [ ! -s "$f" ]; do i=$((i+1)); [ $i -gt 120 ] && exit 1; sleep 2; done
              done
              . /vault/secrets/bootstrap.env
              . /vault/secrets/pg.env
              . /vault/secrets/pg-admin.env
              export PGPASSWORD
              : "${PGHOST:=yb-tserver-service.yugabyte.svc.cluster.local}"
              : "${PGPORT:=5433}"
              : "${PGDATABASE:=kafka}"
              : "${PGUSER:=vaultadmin}"
              apt-get update >/dev/null
              apt-get install -y --no-install-recommends postgresql-client jq >/dev/null
              apt-get clean; rm -rf /var/lib/apt/lists/*

              get_columns() {
                local sch="$1" tbl="$2"
                PGPASSWORD="$PGPASSWORD" \
                psql "host=$PGHOST port=$PGPORT dbname=$PGDATABASE user=$PGUSER sslmode=disable" -Atqc \
                  "SELECT string_agg(quote_ident(column_name), ',')
                   FROM information_schema.columns
                   WHERE table_schema=$$${sch}$$ AND table_name=$$${tbl}$$
                   ORDER BY ordinal_position;" | tr -d '\n'
              }
              check_empty() {
                local sch="$1" tbl="$2"
                local cnt
                cnt="$(PGPASSWORD="$PGPASSWORD" psql "host=$PGHOST port=$PGPORT dbname=$PGDATABASE user=$PGUSER sslmode=disable" -Atqc "SELECT COUNT(*) FROM ${sch}.${tbl};" || echo "0")"
                if [ "$cnt" != "0" ] && [ -z "${ALLOW_NONEMPTY:-}" ]; then exit 1; fi
              }

              SQL_DISCOVER="
              WITH c AS (
                SELECT n.nspname AS schema, c.relname AS table, d.description
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  LEFT JOIN pg_description d ON d.objoid = c.oid AND d.objsubid = 0
                WHERE c.relkind IN ('r','p') AND d.description IS NOT NULL
              ),
              j AS (
                SELECT schema, \"table\",
                       CASE WHEN left(description,1)='{' THEN description::jsonb ELSE NULL END AS jdesc
                FROM c
              )
              SELECT json_build_object('schema', schema, 'table',  \"table\", 'cfg', jdesc -> 'bootstrap')
                FROM j
               WHERE jdesc ? 'bootstrap'
                 AND COALESCE( (jdesc->'bootstrap'->>'enabled')::boolean, false ) = true;
              "
              DISCOVERED="$(PGPASSWORD="$PGPASSWORD" psql "host=$PGHOST port=$PGPORT dbname=$PGDATABASE user=$PGUSER sslmode=disable" -Atqc "$SQL_DISCOVER" || true)"
              echo "$DISCOVERED" | while read -r ROW_JSON; do
                [ -z "$ROW_JSON" ] && continue
                SCH="$(echo "$ROW_JSON" | jq -r '.schema')"
                TBL="$(echo "$ROW_JSON" | jq -r '.table')"
                CFG="$(echo "$ROW_JSON" | jq -c '.cfg // {}')"
                BQ_TABLE="$(echo "$CFG" | jq -r '.bq // empty')"
                [ -z "$BQ_TABLE" ] && exit 1
                PREFIX="$(echo "$CFG" | jq -r '.gcs_prefix // "'$TBL'"')"
                FMT="$(echo "$CFG" | jq -r '.format // "CSV"')"
                HDR="$(echo "$CFG" | jq -r '.header // true')"
                COLS_OVERRIDE="$(echo "$CFG" | jq -r '.columns // empty')"
                if [ -z "$COLS_OVERRIDE" ] || [ "$COLS_OVERRIDE" = "null" ]; then
                  COLS="$(get_columns "$SCH" "$TBL")"
                else
                  COLS="$(echo "$COLS_OVERRIDE" | awk -F',' '{for(i=1;i<=NF;i++){gsub(/^ +| +$/,"",$i); printf "%s\"%s\"", (i>1?",":""), $i}}')"
                fi
                DEST_URI="gs://${BUCKET}/bootstrap/${PREFIX}/${PREFIX}-*.csv"
                if [ -n "${LOCATION:-}" ]; then BQ_LOC_FLAG="--location=${LOCATION}"; else BQ_LOC_FLAG=""; fi
                bq ${BQ_LOC_FLAG} --project_id="${PROJECT}" extract \
                  --destination_format="${FMT}" \
                  --field_delimiter="," \
                  --print_header="${HDR}" \
                  "${BQ_TABLE}" \
                  "${DEST_URI}"
                check_empty "$SCH" "$TBL"
                gsutil cat "gs://${BUCKET}/bootstrap/${PREFIX}/${PREFIX}-*.csv" \
                | PGPASSWORD="$PGPASSWORD" psql "host=$PGHOST port=$PGPORT dbname=$PGDATABASE user=$PGUSER sslmode=disable" \
                    -v ON_ERROR_STOP=1 \
                    -c "\copy ${SCH}.${TBL} (${COLS}) from stdin with (format csv, header ${HDR})"
              done

# ============================================================================
# Kafka (wave 2): single-broker KRaft — reads CLUSTER_ID from Secret
# ============================================================================
---
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
  selector:
    app: kafka
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-hs
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  clusterIP: None
  ports:
    - name: controller
      port: 9093
      targetPort: 9093
  selector:
    app: kafka
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  serviceName: kafka-hs
  replicas: 1
  selector:
    matchLabels: { app: kafka }
  template:
    metadata:
      labels: { app: kafka }
      annotations:
        linkerd.io/inject: "disabled"
    spec:
      initContainers:
        - name: format-storage
          image: apache/kafka:3.9.0
          imagePullPolicy: IfNotPresent
          env:
            - name: CLUSTER_ID
              valueFrom:
                secretKeyRef:
                  name: kafka-cluster-id
                  key: CLUSTER_ID
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -euo pipefail
              META="/tmp/kraft-combined-logs/meta.properties"
              if [ -f "$META" ]; then
                echo "meta.properties present; not reformatting."
                grep -E '^cluster\.id=' "$META" || true
              else
                echo "Formatting storage with cluster.id=${CLUSTER_ID}"
                /opt/kafka/bin/kafka-storage.sh format \
                  -t "${CLUSTER_ID}" \
                  -c /opt/kafka/config/kraft/server.properties \
                  --ignore-formatted
              fi
          volumeMounts:
            - name: kafka-logs
              mountPath: /tmp/kraft-combined-logs
      containers:
        - name: broker
          image: apache/kafka:3.9.0
          imagePullPolicy: IfNotPresent
          # NOTE: do NOT set CLUSTER_ID here; only initContainer formats storage.
          env:
            - name: KAFKA_NODE_ID
              value: "1"
            - name: KAFKA_PROCESS_ROLES
              value: "broker,controller"
            - name: KAFKA_CONTROLLER_LISTENER_NAMES
              value: "CONTROLLER"
            - name: KAFKA_LISTENERS
              value: "PLAINTEXT://:9092,CONTROLLER://:9093"
            - name: KAFKA_ADVERTISED_LISTENERS
              value: "PLAINTEXT://kafka.kafka.svc.cluster.local:9092"
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: "PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT"
            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
              value: "1@kafka-0.kafka-hs.kafka.svc.cluster.local:9093"
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_TRANSACTION_STATE_LOG_MIN_ISR
              value: "1"
            - name: KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS
              value: "0"
            - name: KAFKA_LOG_DIRS
              value: "/tmp/kraft-combined-logs"
          ports:
            - containerPort: 9092
            - containerPort: 9093
          volumeMounts:
            - name: kafka-logs
              mountPath: /tmp/kraft-combined-logs
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
      volumes:
        - name: kafka-logs
          emptyDir: {}

# ============================================================================
# Kafka Connect (wave 3)
# ============================================================================
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-connect
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  ports:
    - name: rest
      port: 8083
      targetPort: 8083
  selector:
    app: kafka-connect
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-connect
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  replicas: 1
  selector:
    matchLabels: { app: kafka-connect }
  template:
    metadata:
      labels: { app: kafka-connect }
      annotations:
        linkerd.io/inject: "disabled"
    spec:
      volumes:
        - name: plugins
          emptyDir: {}
      initContainers:
        - name: install-plugins
          image: confluentinc/cp-kafka-connect:7.6.1
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -e
              mkdir -p /plugins
              apt-get update >/dev/null 2>&1 || true
              apt-get install -y unzip curl >/dev/null 2>&1 || true
              confluent-hub install --no-prompt --component-dir /plugins confluentinc/kafka-connect-bigquery:${BQ_SINK_VER}
              curl -fsSL "${YB_DEBEZIUM_URL}" -o /tmp/yb-connector.zip || wget -qO /tmp/yb-connector.zip "${YB_DEBEZIUM_URL}"
              unzip -o /tmp/yb-connector.zip -d /plugins >/dev/null 2>&1 || true
          env:
            - name: BQ_SINK_VER
              value: "2.6.1"
            - name: YB_DEBEZIUM_URL
              value: "https://downloads.yugabyte.com/debezium/dz.1.9.5.yb.grpc.2024.2.2/debezium-connector-yugabytedb-grpc.zip"
          volumeMounts:
            - name: plugins
              mountPath: /plugins
      containers:
        - name: connect
          image: confluentinc/cp-kafka-connect:7.6.1
          imagePullPolicy: IfNotPresent
          env:
            - name: CONNECT_BOOTSTRAP_SERVERS
              value: "kafka.kafka.svc.cluster.local:9092"
            - name: CONNECT_REST_ADVERTISED_HOST_NAME
              value: "kafka-connect"
            - name: CONNECT_REST_PORT
              value: "8083"
            - name: CONNECT_GROUP_ID
              value: "connect-cluster"
            - name: CONNECT_CONFIG_STORAGE_TOPIC
              value: "_connect-configs"
            - name: CONNECT_OFFSET_STORAGE_TOPIC
              value: "_connect-offsets"
            - name: CONNECT_STATUS_STORAGE_TOPIC
              value: "_connect-status"
            - name: CONNECT_CONFIG_STORAGE_REPLICATION_FACTOR
              value: "1"
            - name: CONNECT_OFFSET_STORAGE_REPLICATION_FACTOR
              value: "1"
            - name: CONNECT_STATUS_STORAGE_REPLICATION_FACTOR
              value: "1"
            - name: CONNECT_KEY_CONVERTER
              value: "org.apache.kafka.connect.json.JsonConverter"
            - name: CONNECT_VALUE_CONVERTER
              value: "org.apache.kafka.connect.json.JsonConverter"
            - name: CONNECT_KEY_CONVERTER_SCHEMAS_ENABLE
              value: "true"
            - name: CONNECT_VALUE_CONVERTER_SCHEMAS_ENABLE
              value: "true"
            - name: CONNECT_PLUGIN_PATH
              value: "/plugins"
            - name: KAFKA_HEAP_OPTS
              value: "-Xms256m -Xmx512m"
          ports:
            - containerPort: 8083
          readinessProbe:
            httpGet: { path: /connectors, port: 8083 }
            initialDelaySeconds: 20
            periodSeconds: 5
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
          volumeMounts:
            - name: plugins
              mountPath: /plugins

# ============================================================================
# Registration Job (wave 5)
# ============================================================================
---
apiVersion: batch/v1
kind: Job
metadata:
  name: register-yb-to-bq
  namespace: kafka
  annotations:
    argocd.argoproj.io/sync-wave: "5"
    vault.hashicorp.com/agent-inject: "true"
    vault.hashicorp.com/role: "data-bootstrap"
    vault.hashicorp.com/agent-inject-secret-yb: "secret/data/kafka/pgconfig"
    vault.hashicorp.com/agent-inject-template-yb: |
      {{- with secret "secret/data/kafka/pgconfig" -}}
      {{ with .Data.data.host }}PGHOST={{ . }}{{ end }}
      {{ with .Data.data.port }}PGPORT={{ . }}{{ end }}
      {{ with .Data.data.database }}PGDATABASE={{ . }}{{ end }}
      {{ with .Data.data.user }}PGUSER={{ . }}{{ end }}
      {{ with .Data.data.master_addresses }}YB_MASTER_ADDRESSES={{ . }}{{ end }}
      {{- end }}
    vault.hashicorp.com/agent-inject-secret-bq: "secret/data/kafka/bq/config"
    vault.hashicorp.com/agent-inject-template-bq: |
      {{- with secret "secret/data/kafka/bq/config" -}}
      {{ with .Data.data.project }}BQ_PROJECT={{ . }}{{ end }}
      {{ with .Data.data.dataset }}BQ_DEFAULT_DATASET={{ . }}{{ end }}
      {{ with .Data.data.sa_json }}BQ_KEY_JSON={{ . | toJSON }}{{ end }}
      {{- end }}
    vault.hashicorp.com/agent-inject-secret-pgpass.env: "secret/data/vaultadmin"
    vault.hashicorp.com/agent-inject-template-pgpass.env: |
      {{- with secret "secret/data/vaultadmin" -}}
      PGPASSWORD={{ .Data.data.value }}
      {{- end }}
spec:
  template:
    metadata:
      annotations:
        linkerd.io/inject: "disabled"
    spec:
      serviceAccountName: bq-yb-bootstrap
      restartPolicy: OnFailure
      initContainers:
        - name: wait-bootstrap-ok
          image: alpine:3.19
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -e
              API="https://kubernetes.default.svc"
              TOKEN="$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)"
              CACERT="/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
              until [ "$(wget -qO- --header="Authorization: Bearer ${TOKEN}" --ca-certificate="${CACERT}" \
                "${API}/api/v1/namespaces/kafka/configmaps/bootstrap.ok" \
                | sed -n 's/.*"status":"\([^"]*\)".*/\1/p')" = "complete" ]; do
                sleep 5
              done
      containers:
        - name: registrar
          image: yugabytedb/yugabyte:2024.2.2.0-b105
          command: ["/bin/bash","-lc"]
          args:
            - |
              set -euo pipefail
              for f in /vault/secrets/yb /vault/secrets/bq /vault/secrets/pgpass.env; do
                i=0; while [ ! -s "$f" ]; do i=$((i+1)); [ $i -gt 120 ] && exit 1; sleep 2; done
                . "$f"
              done
              export PGPASSWORD

              until wget -qO- http://kafka-connect.kafka.svc.cluster.local:8083/connectors >/dev/null 2>&1; do
                sleep 5
              done

              PGURL="postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}?sslmode=disable"

              mapfile -t ROWS < <(
                /home/yugabyte/bin/ysqlsh "${PGURL}" -Atc "
                  with t as (
                    select table_schema, table_name,
                           obj_description((quote_ident(table_schema)||'.'||quote_ident(table_name))::regclass) as comment
                    from information_schema.tables
                    where table_type='BASE TABLE'
                      and table_schema not in ('pg_catalog','information_schema')
                  )
                  select table_schema||','||table_name||','||coalesce( (comment::jsonb -> 'bootstrap' ->> 'bq'), '' )
                  from t
                  where comment is not null
                    and ((comment::jsonb -> 'bootstrap' ->> 'enabled')::boolean is true);
                "
              )
              if [ ${#ROWS[@]} -eq 0 ]; then
                echo "No flagged tables found; exiting."
                exit 0
              fi

              SERVER_NAME="yb"
              TABLE_INCLUDE_LIST=""
              TOPICS=""
              TOPIC2TABLEMAP=""
              for R in "${ROWS[@]}"; do
                SCHEMA="$(echo "$R" | cut -d, -f1)"
                TBL="$(echo "$R" | cut -d, -f2)"
                BQMAP="$(echo "$R" | cut -d, -f3-)"
                FQ="${SCHEMA}.${TBL}"
                TOPIC="${SERVER_NAME}.${FQ}"
                TABLE_INCLUDE_LIST="${TABLE_INCLUDE_LIST}${FQ},"
                TOPICS="${TOPICS}${TOPIC},"
                if [ -n "$BQMAP" ] && [[ "$BQMAP" == *.* ]]; then
                  TOPIC2TABLEMAP="${TOPIC2TABLEMAP}${TOPIC}:${BQMAP},"
                else
                  TOPIC2TABLEMAP="${TOPIC2TABLEMAP}${TOPIC}:${BQ_DEFAULT_DATASET}.${TBL},"
                fi
              done
              TABLE_INCLUDE_LIST="${TABLE_INCLUDE_LIST%,}"
              TOPICS="${TOPICS%,}"
              TOPIC2TABLEMAP="${TOPIC2TABLEMAP%,}"

              cat > /tmp/yb-source.json <<JSON
              {
                "name": "yb-source",
                "config": {
                  "connector.class": "io.debezium.connector.yugabytedb.YugabyteDBgRPCConnector",
                  "tasks.max": "1",
                  "database.hostname": "${PGHOST}",
                  "database.port": "${PGPORT}",
                  "database.master.addresses": "${YB_MASTER_ADDRESSES}",
                  "database.user": "${PGUSER}",
                  "database.password": "${PGPASSWORD}",
                  "database.dbname": "${PGDATABASE}",
                  "database.server.name": "${SERVER_NAME}",
                  "table.include.list": "${TABLE_INCLUDE_LIST}",
                  "snapshot.mode": "never",
                  "transforms": "unwrap",
                  "transforms.unwrap.type": "io.debezium.connector.yugabytedb.transforms.YBExtractNewRecordState",
                  "topic.creation.default.replication.factor": "1",
                  "topic.creation.default.partitions": "1",
                  "topic.creation.default.cleanup.policy": "delete"
                }
              }
              JSON
              wget --method=PUT --header="Content-Type: application/json" \
                   --body-file=/tmp/yb-source.json \
                   -qO- http://kafka-connect.kafka.svc.cluster.local:8083/connectors/yb-source/config >/dev/null 2>&1 \
              || wget --header="Content-Type: application/json" \
                      --post-file=/tmp/yb-source.json \
                      -qO- http://kafka-connect.kafka.svc.cluster.local:8083/connectors >/dev/null

              cat > /tmp/bq-sink.json <<JSON
              {
                "name": "bq-sink",
                "config": {
                  "connector.class": "com.wepay.kafka.connect.bigquery.BigQuerySinkConnector",
                  "tasks.max": "1",
                  "topics": "${TOPICS}",
                  "topic2TableMap": "${TOPIC2TABLEMAP}",
                  "project": "${BQ_PROJECT}",
                  "defaultDataset": "${BQ_DEFAULT_DATASET}",
                  "sanitizeTopics": "true",
                  "autoCreateTables": "false",
                  "allowNewBigQueryFields": "true",
                  "allowBigQueryRequiredFieldRelaxation": "true",
                  "keySource": "JSON",
                  "keyfile": ${BQ_KEY_JSON}
                }
              }
              JSON
              wget --method=PUT --header="Content-Type: application/json" \
                   --body-file=/tmp/bq-sink.json \
                   -qO- http://kafka-connect.kafka.svc.cluster.local:8083/connectors/bq-sink/config >/devnull 2>&1 \
              || wget --header="Content-Type: application/json" \
                      --post-file=/tmp/bq-sink.json \
                      -qO- http://kafka-connect.kafka.svc.cluster.local:8083/connectors >/dev/null
          resources:
            requests: { cpu: "200m", memory: "256Mi" }
            limits:   { cpu: "1000m", memory: "512Mi" }
